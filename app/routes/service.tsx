import { useEffect,useState } from 'react';
import { Link,useLoaderData,useNavigate } from 'react-router';
import type { Route } from './+types/service';
import { Heart,MapPin,Clock,ArrowRight,Check,LockKeyhole,CalendarDays,Star } from 'lucide-react';
import { serverApi } from '../lib/server.server';
import { useApp } from '../lib/context';
import { apiRequest,post } from '../lib/api';
import type { Service,Merchant,Booking } from '../lib/types';
import { money,time,localDate,friendlyDate,STYLE_ZH } from '../lib/types';
import { PageHeader,Modal,ErrorNotice,Loading } from '../components/ui';
export const loader=({request,context,params}:Route.LoaderArgs)=>serverApi<{service:Service;merchant:Merchant;reviews:{id:string;rating:number;body:string;name:string}[]}>(request,context,`/api/services/${params.id}`);
export const meta=({loaderData}:Route.MetaArgs)=>[{title:`${loaderData?.service.name??'Service'} — Hotlah`}];
export default function ServicePage(){
 const {service:s,merchant:m,reviews}=useLoaderData<typeof loader>();const {t,lang,saved,toggleSaved,session,setAuthOpen,toast}=useApp();const navigate=useNavigate();
 const [date,setDate]=useState(localDate(1)),[slots,setSlots]=useState<Record<string,{minute:number;available:boolean}[]>>({}),[loading,setLoading]=useState(true),[error,setError]=useState(''),[selection,setSelection]=useState<{date:string;minute:number}|null>(null),[open,setOpen]=useState(false),[message,setMessage]=useState(''),[busy,setBusy]=useState(false),[requestKey,setRequestKey]=useState('');
 const dates=Array.from({length:3},(_,i)=>{const d=new Date(`${date}T12:00:00Z`);d.setUTCDate(d.getUTCDate()+i);return d.toISOString().slice(0,10);});
 useEffect(()=>{let active=true;setLoading(true);setError('');Promise.all(dates.map(d=>apiRequest<{slots:{minute:number;available:boolean}[]}>(`/api/services/${s.id}/availability?date=${d}`).then(v=>[d,v.slots] as const))).then(rows=>{if(active)setSlots(Object.fromEntries(rows));}).catch(e=>{if(active)setError(e.message);}).finally(()=>{if(active)setLoading(false);});return()=>{active=false;};},[date,s.id]);
 useEffect(()=>{post('/api/events',{merchantId:s.merchant_id,kind:'view'}).catch(()=>{});},[s.merchant_id]);
 const request=async()=>{setBusy(true);setError('');try{const result=await post<{booking:Booking}>('/api/bookings',{serviceId:s.id,date:selection!.date,minute:selection!.minute,message,requestKey});setOpen(false);navigate(`/bookings/${result.booking.id}`);toast(t('Your request is on its way.','预约请求已发送。'));}catch(e){setError((e as Error).message);}finally{setBusy(false);}};
 const allTimes=[...new Set(Object.values(slots).flat().map(v=>v.minute))].sort((a,b)=>a-b);
 return <div className="detail-page"><PageHeader title={t('The details','服务详情')} action={<button className="icon-button" aria-label="Save service" aria-pressed={saved.includes(s.id)} onClick={()=>toggleSaved(s.id)}><Heart size={22} fill={saved.includes(s.id)?'currentColor':'none'}/></button>}/><div className="detail-layout"><div className="detail-story"><img className="detail-photo" src={s.image} width="1000" height="750" alt={t(`${s.style} manicure example`,`${STYLE_ZH[s.style]??s.style}美甲示例`)}/><div className="detail-copy"><div className="tags"><span>{lang==='zh'?STYLE_ZH[s.style]:s.style}</span><span>{s.shape}</span></div><h1>{lang==='zh'?s.name_zh:s.name}</h1><p className="detail-price">{money(s.price)} <span><Clock size={15}/>{s.duration} {t('min','分钟')}</span></p><p className="description">{s.description}</p><div className="nailist-line"><div className="avatar">{m.name.slice(0,1)}</div><div><Link className="nailist-profile-link" to={`/nailists/${m.id}`}>{m.name}</Link><p className="location-line"><MapPin size={14}/>{m.area} · {t(m.type==='home'?'Home studio':m.type==='mobile'?'Mobile nailist':'Nail studio',m.type==='home'?'家庭工作室':m.type==='mobile'?'上门服务':'美甲店')}</p></div><span className="verified"><Check size={14}/>{t('Approved','已审核')}</span></div><p className="muted">{m.bio}</p>{m.type==='home'&&<p className="privacy-note"><LockKeyhole size={16}/>{t('Exact address shared after your booking is approved.','预约确认后提供详细地址。')}</p>}<details className="detail-disclosure"><summary>{t('Before you book','预约须知')}</summary><p>{m.policy}</p><p>{t('Pay your nailist at the appointment. Hotlah does not collect your service payment.','服务费用在预约当天直接支付给美甲师，Hotlah 不收取服务费。')}</p></details><details className="detail-disclosure"><summary>{t(`Reviews (${reviews.length})`,`评价 (${reviews.length})`)}</summary>{reviews.length?reviews.map(r=><div className="review" key={r.id}><strong>{r.name}</strong><span><Star size={14}/> {r.rating}/5</span><p>{r.body}</p></div>):<p className="muted">{t('A new discovery. Be the first to leave a review after your appointment.','新的发现。完成预约后，留下第一条评价。')}</p>}</details></div></div><aside className="availability-panel"><div className="section-heading"><h2>{t('Find your time','选择预约时间')}</h2><CalendarDays size={23}/></div><p className="muted small">{t('Choose a time to request. All times are Malaysia time.','选择心仪时段，提交预约。所有时间为马来西亚时间。')}</p><label className="field date-field">{t('Starting from','起始日期')}<input type="date" min={localDate()} max={localDate(88)} value={date} onChange={e=>{if(e.target.value){setDate(e.target.value);setSelection(null);}}}/></label>{error&&!open&&<ErrorNotice message={error}/>} {loading?<Loading/>:<div className="availability-scroll"><table className="availability-table"><thead><tr>{dates.map(d=><th key={d}>{friendlyDate(d,lang)}</th>)}</tr></thead><tbody>{allTimes.map(minute=><tr key={minute}>{dates.map(d=>{const available=slots[d]?.some(s=>s.minute===minute&&s.available);const selected=selection?.date===d&&selection.minute===minute;return <td key={d}><button disabled={!available} className={selected?'selected':''} aria-pressed={selected} aria-label={`${friendlyDate(d)} ${time(minute)}`} onClick={()=>setSelection({date:d,minute})}>{available?time(minute):'—'}</button></td>;})}</tr>)}</tbody></table>{!allTimes.length&&<p>{t('No times available. Try another date.','暂无时段，请选择其他日期。')}</p>}</div>}<div className="booking-promise"><Check size={17}/><span>{t('Pay at your appointment','预约当天付款')}</span></div><p className="small muted">{t('Your nailist will confirm the request. You can cancel from your booking.','美甲师将确认您的预约。您可在预约页面取消。')}</p><div className="booking-cta"><div><strong>{money(s.price)}</strong><small>{selection?`${friendlyDate(selection.date,lang)} · ${time(selection.minute)}`:t('Select a time above','请先选择时间')}</small></div><button className="button primary" disabled={!selection} onClick={()=>{if(!session.user){setAuthOpen(true);return;}setRequestKey(crypto.randomUUID());setError('');setOpen(true);}}>{t('Request','预约')}<ArrowRight size={18}/></button></div></aside></div><Modal open={open} onOpenChange={v=>{if(!busy)setOpen(v);}} title={t('One step closer','离心仪美甲更近一步')}><div className="request-summary"><img src={s.image} alt=""/><div><h3>{lang==='zh'?s.name_zh:s.name}</h3><p>{m.name}</p><strong>{money(s.price)}</strong></div></div><p>{selection&&`${friendlyDate(selection.date,lang)} · ${time(selection.minute)}`}</p><label className="field">{t('A note for your nailist (optional)','给美甲师留言（选填）')}<textarea rows={4} maxLength={1500} value={message} onChange={e=>setMessage(e.target.value)} placeholder={t('A shorter shape? A different colour? Let them know.','想要短一点的甲型或其他颜色？告诉美甲师吧。')}/></label><p className="small muted">{t('No payment now. Your appointment is confirmed only after approval.','现在无需付款。美甲师批准后，预约才算确认。')}</p>{error&&<ErrorNotice message={error}/>}<button className="button primary full" disabled={busy} onClick={request}>{t(busy?'Sending…':'Send booking request',busy?'发送中…':'发送预约请求')}<ArrowRight size={18}/></button></Modal></div>;
}
